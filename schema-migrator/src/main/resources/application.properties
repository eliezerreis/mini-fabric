server.port=8083

spring.application.name=schema-migrator

schema.registry.url=${SCHEMA_REGISTRY_URL:http://localhost:8081}

management.endpoints.web.exposure.include=health,info

logging.level.com.myfabric=DEBUG
